include_directories("${MATLAB_ROOT}/extern/include")
if(WIN32)
    if(CMAKE_CL_64)
        link_directories(${MATLAB_ROOT}/bin/win64)
        message("win64")
    else()
        link_directories(${MATLAB_ROOT}/bin/win32)
        message("win32")
    endif()
elseif(UNIX)
    link_directories(${MATLAB_ROOT}/bin/glnxa64)
    message("unix")
endif()

matlab_mex(hello hello.c)
matlab_mex(hellocpp hellocpp.cpp)
matlab_mex(explore explore.c)
